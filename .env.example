SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key

# Optionnel — crash reporting (laisser vide pour désactiver)
SENTRY_DSN=

# Optionnel — abonnements RevenueCat (clé API publique iOS, préfixe "appl_")
# Laisser vide tant que RevenueCat n'est pas configuré → app en mode gratuit.
REVENUECAT_API_KEY_IOS=
